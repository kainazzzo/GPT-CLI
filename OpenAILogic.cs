namespace GptConsoleApp;

public class OpenAILogic
{
    private readonly string _apiKey;
    private readonly string _baseUrl;

    public OpenAILogic(string apiKey, string baseUrl)
    {
        _apiKey = apiKey;
        _baseUrl = baseUrl;
    }

    // Add methods to interact with the OpenAI API here
}
